import styled from "styled-components"

  const StyledFooter = styled.footer`
    height: 160px;
    background-color: #ffffff;
  `
const Footer = () => {
  return <StyledFooter>页脚</StyledFooter>
}

export default Footer
